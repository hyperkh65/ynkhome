name: Deploy to NAS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Copy build to NAS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: hy64.synology.me
          username: urjent
          key: ${{ secrets.NAS_SSH_PRIVATE_KEY }}
          source: ".next/standalone/,.next/static/,public/"
          target: /volume1/web/ynkhome/

      - name: Restart server on NAS
        uses: appleboy/ssh-action@v1
        with:
          host: hy64.synology.me
          username: urjent
          key: ${{ secrets.NAS_SSH_PRIVATE_KEY }}
          command_timeout: 2m
          script_stop: false
          script: |
            NODE=/volume1/homes/urjent/runtime/node-v22.16.0-linux-x64/bin/node

            mkdir -p /volume1/web/ynkhome/.next/standalone/.next

            cp -r /volume1/web/ynkhome/.next/static /volume1/web/ynkhome/.next/standalone/.next/static 2>/dev/null
            cp -r /volume1/web/ynkhome/public /volume1/web/ynkhome/.next/standalone/public 2>/dev/null

            kill $(cat /tmp/ynkhome.pid 2>/dev/null) 2>/dev/null

            PORT=3102 $NODE /volume1/web/ynkhome/.next/standalone/server.js >> /tmp/ynkhome.log 2>&1 &
            echo $! > /tmp/ynkhome.pid
            echo "Started PID=$(cat /tmp/ynkhome.pid)"
            exit 0
